[project]
name = "lociapro"
version = "0.1.0"
requires-python = ">=3.11"

[tool.uv.workspace]
members = ["datos", "aplicacion", "presentacion"]

[tool.uv]
dev-dependencies = ["ruff>=0.4", "pytest>=8.0"]
